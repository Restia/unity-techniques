using UnityEngine;
using System.Collections;

public class KLVController : MonoBehaviour {

	// Use this for initialization
	void Start () {
        GetComponent<KLVDieable>().Die();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
